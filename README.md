# Fantasy Quest Clicker

Мобильное RPG-приложение в жанре кликер, разработанное на Kotlin для Android.

## 🎯 Статус проекта

**✅ Полностью реализовано:**

- Чистая архитектура (UI Layer - Domain Layer - Data Layer)
- Боевая система с тап-управлением и авто-атаками
- Система экономики (золото, прокачка характеристик)
- 5 типов постоянных квестов с системой достижений
- Генерация врагов (Гоблин, Орк, Скелет, боссы)
- Сохранение прогресса через DataStore
- Кастомные UI компоненты (HealthBar, QuestBar, BaseScreen)
- Форматирование чисел (1K, 1M)
- Модульное тестирование ключевой логики

## 🎮 Игровой процесс

- Тап по врагу для атаки + система авто-атак
- Health Bar с цветовой индикацией состояния
- Смена врагов при победе, появление боссов
- Начисление золота и опыт за победы
- 5 типов квестов: убийства, стадии, улучшения, золото
- Система прокачки (атака, крит. шанс, время)
- Сохранение прогресса между сессиями

## 🏗️ Архитектура

### Clean Architecture + MVVM + StateFlow

**🎨 UI Layer:**
- `BattleScreen`, `SkillsScreen`, `QuestsScreen` - игровые экраны
- `BattleViewModel` - управление состоянием боя
- `HealthBar`, `QuestBar` - кастомные компоненты
- `BaseScreen` → `BaseGameScreen` - иерархия шаблонов

**🧠 Domain Layer:**
- `AttackEnemyUseCase` - бизнес-логика атаки
- `Player`, `Enemy`, `Quest` - доменные модели
- `QuestGenerator` - генерация системы квестов

**💾 Data Layer:**
- `PlayerRepository`, `QuestRepository` - реализация с DataStore
- `DataStore<Preferences>` - локальное хранилище прогресса

## 📚 Документация кода

Проект документирован с использованием **KDoc** и **Dokka 2.1.0**. Документация генерируется автоматически и доступна в двух форматах:

### 🚀 Быстрый старт с документацией
```bash
# Сгенерировать HTML-документацию
./gradlew dokkaHtml

# Документация будет создана в папке:
# app/build/dokka/html/
```

### 📁 Структура документации
- **Модели данных** (Player, Enemy, Quest) — описание полей и методов
- **Use Cases** (AttackEnemyUseCase) — бизнес-логика с примерами
- **Репозитории** (PlayerRepository) — работа с данными
- **ViewModel** (BattleViewModel) — управление состоянием экранов
- **UI компоненты** (BattleScreen, HealthBar) — описание Composable-функций

## 🛠 Технологический стек

- **Язык:** Kotlin + Coroutines + Flow
- **Минимальная SDK:** Android 8.0 (API 26)
- **Архитектура:** Clean Architecture + MVVM
- **UI:** Jetpack Compose + Material3
- **Состояние:** StateFlow + Combine
- **Локальная БД:** DataStore (Preferences)
- **Тестирование:** JUnit
- **Сборка:** Gradle с Kotlin DSL

## ✅ Ключевые особенности

- **Реактивная архитектура** - StateFlow + Combine для синхронизации состояния
- **Чистая архитектура** - четкое разделение ответственности между слоями
- **Протестированная логика** - юнит-тесты покрывают Use Cases и генераторы
- **Самодокументируемый код** - комментарии для сложной бизнес-логики
- **Процедурная генерация** - враги, квесты, цены прокачки

## 🚀 Сборка и запуск

```bash
./gradlew assembleDebug
```

**Минимальные требования: Android 8.0+ (API 26), 100MB свободной памяти**

**Разработано:** Епифанов М.О. (ИКБО-51-23)
**Руководитель:** Золотухин С.А.

**РТУ МИРЭА, 2025**
